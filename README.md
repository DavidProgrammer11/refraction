🧪 How It Works
The app uses Snell's Law:

𝑛1⋅sin(𝑖) = 𝑛2⋅sin(𝑟)
Depending on which field is left blank, the program calculates the missing variable using trigonometric functions from the math and numpy libraries.

🛠️ Technologies Used
Python 3
Tkinter for GUI
NumPy for trigonometric calculations

📸 UI Preview
The interface includes:

Input fields for n1, n2, i (Ray), and r (Ray)
A "Calcular" button to trigger the computation
A result display area

🚀 Getting Started
To run the program:

Make sure you have Python and NumPy installed.
Run the script using python optics_calculator.py.
Enter known values and leave one field blank to calculate it.
